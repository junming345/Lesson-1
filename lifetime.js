var currentage = 23;
var maxage = 90;
var estimatedintake = 3;
var total = (maxage - currentage) * estimatedintake;
console.log("You will need " + total + " to last you until the ripe old age of " + maxage);